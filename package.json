{
  "name": "imgsearch",
  "version": "1.0.0",
  "description": "+ I can pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp or a natural language date (example: January 1, 2016) + If it does, it returns both the Unix timestamp and the natural language form of that date. + If it does not contain a date or Unix timestamp, it returns null for those properties.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kurumkan/imgsearch.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kurumkan/imgsearch/issues"
  },
  "homepage": "https://github.com/kurumkan/imgsearch#readme",
  "dependencies": {
    "bing.search": "^1.0.1",
    "ejs": "^2.5.2",
    "express": "^4.14.0",
    "mongoose": "^4.6.5"
  }
}
